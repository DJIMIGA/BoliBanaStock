[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs-18_x"]

[phases.install]
cmds = [
    "pip install --no-cache-dir --upgrade pip",
    "pip install --no-cache-dir -r requirements.txt",
    "pip install --no-cache-dir requests>=2.31.0",
    "pip install --no-cache-dir sendgrid>=6.10.0",
    "echo 'Python dependencies installed successfully'",
    "python -c 'import sendgrid; print(f\"✅ SendGrid installé: version {sendgrid.__version__}\")' || echo '⚠️ SendGrid non trouvé après installation'",
    "pip list | grep -i sendgrid || echo '⚠️ SendGrid non trouvé dans pip list'",
    "echo '🎨 Vérification de Node.js et npm pour Tailwind CSS...'",
    "node --version && echo '✅ Node.js disponible' || echo '❌ Node.js non trouvé'",
    "npm --version && echo '✅ npm disponible' || echo '❌ npm non trouvé'",
    "echo '📁 Vérification des répertoires et fichiers Tailwind...'",
    "test -d theme && echo '✅ Répertoire theme existe' || echo '❌ Répertoire theme manquant'",
    "test -f static/css/src/input.css && echo '✅ input.css existe' || echo '❌ input.css manquant'",
    "test -f theme/package.json && echo '✅ package.json existe' || echo '❌ package.json manquant'",
    "mkdir -p static/css/dist && echo '✅ Répertoire dist créé'",
    "cd theme && echo '📦 Installation des dépendances npm...' && npm install",
    "cd theme && echo '📋 Liste des packages npm installés:' && npm list --depth=0 || echo '⚠️ Impossible de lister les packages npm'"
]

[phases.build]
cmds = [
    "echo '🎨 Phase BUILD - Génération de Tailwind CSS...'",
    "echo '📁 Répertoire de travail:' && pwd",
    "echo '📁 Vérification de Node.js et npm dans la phase BUILD:'",
    "node --version || echo '❌ Node.js non disponible'",
    "npm --version || echo '❌ npm non disponible'",
    "echo '📁 Vérification des répertoires:'",
    "test -d theme && echo '✅ Répertoire theme existe' || echo '❌ Répertoire theme manquant'",
    "test -f static/css/src/input.css && echo '✅ input.css existe' || echo '❌ input.css manquant'",
    "test -f theme/package.json && echo '✅ package.json existe' || echo '❌ package.json manquant'",
    "echo '📁 Contenu du répertoire theme:' && ls -la theme/ || echo '⚠️ Impossible de lister theme/'",
    "echo '📁 Vérification de node_modules:' && test -d theme/node_modules && echo '✅ node_modules existe' || echo '⚠️ node_modules n'existe pas (sera installé si nécessaire)'",
    "echo '📁 Contenu de static/css/dist avant build:' && ls -la static/css/dist/ 2>/dev/null || echo '⚠️ Répertoire dist vide ou inexistant'",
    "echo '🔨 Exécution de npm run build dans theme/...'",
    "cd theme && npm run build 2>&1 && cd .. || (echo '❌ Erreur lors de npm run build' && cd .. && exit 1)",
    "echo '📁 Contenu de static/css/dist après build:' && ls -la static/css/dist/ || echo '⚠️ Impossible de lister dist/'",
    "test -f static/css/dist/output.css && echo '✅ Tailwind CSS généré avec succès: static/css/dist/output.css' && ls -lh static/css/dist/output.css || (echo '❌ Erreur: output.css non généré après npm run build' && echo '📋 Vérification du répertoire theme:' && ls -la theme/ && echo '📋 Vérification du répertoire static/css/dist:' && ls -la static/css/dist/ 2>/dev/null || echo 'Répertoire dist n'existe pas' && exit 1)",
    "echo '📦 Collecte des fichiers statiques avec collectstatic...'",
    "DJANGO_SETTINGS_MODULE=bolibanastock.settings_railway python manage.py collectstatic --noinput",
    "echo '📁 Vérification de la collecte de output.css:' && test -f staticfiles/css/dist/output.css && echo '✅ output.css collecté dans staticfiles/' && ls -lh staticfiles/css/dist/output.css || echo '⚠️ output.css non trouvé dans staticfiles/'"
]

[start]
cmd = "python deploy_railway.py && gunicorn bolibanastock.wsgi:application --bind 0.0.0.0:$PORT"
